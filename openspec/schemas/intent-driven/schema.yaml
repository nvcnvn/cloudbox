name: intent-driven
version: 1
description: Intent-driven workflow - proposal → specs (Markdown with fenced Gherkin) → design → adr → tasks
artifacts:
  - id: proposal
    generates: proposal.md
    description: Initial proposal document outlining the change
    template: proposal.md
    instruction: >
      Create the proposal document that establishes WHY this change is needed.


      Sections:

      - **Why**: 1-2 sentences on the problem or opportunity. What problem does
      this solve? Why now?

      - **What Changes**: Bullet list of changes. Be specific about new
      capabilities, modifications, or removals. Mark breaking changes with
      **BREAKING**.

      - **Capabilities**: Identify which specs will be created or modified:
        - **New Capabilities**: List capabilities being introduced. Each becomes a new `specs/<name>/spec.md`. Use kebab-case names (e.g., `user-auth`, `data-export`).
        - **Modified Capabilities**: List existing capabilities whose REQUIREMENTS are changing. Only include if spec-level behaviour changes (not just implementation details). Each needs a delta spec file. Check `openspec/specs/` for existing spec names. Leave empty if no requirement changes.
      - **Impact**: Affected code, APIs, dependencies, or systems.


      IMPORTANT: The Capabilities section is critical. It creates the contract
      between proposal and specs phases. Research existing specs before filling
      this in. Each capability listed here will need a corresponding spec file.


      Durable architectural decisions worth carrying across changes are
      distilled into ADR files at <repo>/adr/ by a later step after design.


      Keep it concise (1-2 pages). Focus on the "why" not the "how" -
      implementation details belong in design.md.


      This is the foundation - specs, design, adr, and tasks all build on this.
    requires: []
  - id: specs
    generates: "specs/**/spec.md"
    description: Detailed specifications for the change (Markdown with fenced Gherkin)
    template: spec.md
    instruction: |
      Create `spec.md` spec files that define WHAT the system should do.

      Create one `spec.md` per capability listed in the proposal's Capabilities
      section — a standard Markdown document: prose (context, rationale, links)
      may appear anywhere, and ALL executable Gherkin lives inside fenced code
      blocks.

      Paths:
      - New capabilities: use the exact kebab-case name from the proposal
        (specs/<capability>/spec.md).
      - Modified capabilities: use the existing spec folder name from
        openspec/specs/<capability>/ when creating the delta spec at
        specs/<capability>/spec.md.

      Fence contract (the extractor and the acceptance-test runner depend on it):
      - Gherkin is executable ONLY inside fences opened by ```gherkin at
        column 0 (info string exactly `gherkin`, nothing more) and closed by
        ``` at column 0. An indented ```gherkin fence is a hard extraction
        error; gherkin quoted inside any other fence is treated as prose.
      - Every spec.md MUST contain at least one gherkin fence.
      - Multiple gherkin fences concatenate in file order and MUST parse as
        exactly ONE top-level `Feature:` — the first fence opens with the
        `Feature:` line (tags, if any, in the same fence immediately above it).

      Gherkin structure (how Gherkin keywords map onto OpenSpec concepts):
      - **Feature:** — the spec file / capability. Exactly one per spec.md.
      - **Rule:** — one requirement. Each `Rule:` is followed by a short
        description line. Use SHALL/MUST for normative requirements (avoid
        should/may).
      - **Scenario:** — a concrete example, written with Given/When/Then steps.
      - Every `Rule:` MUST have at least one `Scenario:`.

      Delta operations use an inline `# @openspec:` marker comment placed
      INSIDE a gherkin fence, immediately ABOVE the `Rule:` it applies to:
      - `# @openspec: ADDED` — new requirement (include full Rule + scenarios)
      - `# @openspec: MODIFIED` — changed behaviour (include the FULL updated
        Rule + all scenarios, not a partial edit)
      - `# @openspec: REMOVED` — deprecated requirement (the `Rule:` line
        naming it is enough)
      - `# @openspec: RENAMED from="<old name>" to="<new name>"` — name change
        only

      MODIFIED workflow: locate the existing `Rule:` in
      openspec/specs/<capability>/spec.md, copy the ENTIRE block (Rule
      description through all scenarios), place it under `# @openspec: MODIFIED`,
      and edit. Partial MODIFIED content loses detail at archive time — if adding
      new concerns without changing existing behaviour, use ADDED instead.

      Example delta spec.md (prose + fence):

      ````markdown
      # User authentication changes

      Login is being tightened: accounts must verify their email address
      before they may log in. See proposal.md for motivation.

      ```gherkin
      Feature: User authentication changes

        # @openspec: ADDED
        Rule: Email must be verified before login
          Unverified accounts MUST NOT be able to log in to the system.

          Scenario: Unverified user is blocked
            Given an account with an unverified email address
            When the user attempts to log in
            Then access is denied

        # @openspec: REMOVED
        Rule: Session expires after inactivity

        # @openspec: RENAMED from="Password must meet complexity requirements" to="Password complexity is enforced"
      ```
      ````

      Specs should be testable - each scenario is a potential test case. The
      prose renders normally on GitHub; the fences are exactly what the
      acceptance suite executes.

      Before considering a spec complete, extract the Gherkin and lint it with
      gherkin-lint, fixing every reported issue. Linting is the same for every
      stack, so a spec that lints clean does so regardless of which runner the
      project uses. See the acceptance-test-authoring skill for your stack's
      extraction + lint commands and their required flags.
    requires:
      - proposal
  - id: design
    generates: design.md
    description: Technical design document with implementation details
    template: design.md
    instruction: >
      Create the design document that explains HOW to implement the change.


      When to include design.md (create only if any apply):

      - Cross-cutting change (multiple services/modules) or new architectural
      pattern

      - New external dependency or significant data model changes

      - Security, performance, or migration complexity

      - Ambiguity that benefits from technical decisions before coding


      Prior decisions (read existing ADRs first):

      Before writing, list every file in <repo>/adr/. Build the supersession
      graph from each ADR's Supersedes field - an ADR is no longer in force if
      a later ADR's Supersedes points at it. Only the currently-in-force ADRs
      (accepted, not superseded) constrain this design. Older superseded ADRs
      are historical context only and MUST NOT be treated as live commitments.
      New design choices MUST be coherent with the in-force set. If a current
      ADR needs to be revisited, do not propose modifying it; flag it under
      Open Questions and let the adr step record a new superseding ADR.


      Sections:

      - **Context**: Background, current state, constraints, stakeholders

      - **Goals / Non-Goals**: What this design achieves and explicitly excludes

      - **Decisions**: Key technical choices with rationale (why X over Y?).
      Include alternatives considered for each decision.

      - **Risks / Trade-offs**: Known limitations, things that could go wrong.
      Format: [Risk] -> Mitigation

      - **Migration Plan**: Steps to deploy, rollback strategy (if applicable)

      - **Open Questions**: Outstanding decisions or unknowns to resolve.
      Include any in-force ADRs that this design suggests revisiting; the adr
      step will record the supersession.


      Focus on architecture and approach, not line-by-line implementation.

      Reference the proposal for motivation and specs for requirements.


      Good design docs explain the "why" behind technical decisions.
    requires:
      - proposal
  - id: adr
    generates: adr.md
    description: Per-change ADR review manifest and durable decision handoff
    template: adr.md
    instruction: >
      Complete ADR review for this change by writing a concise manifest at
      openspec/changes/<change>/adr.md. This change-local file is the artifact
      completion marker for the adr step. Existing files under <repo>/adr/ are
      required context for the review, but they MUST NOT satisfy completion for
      a new change.


      If the change introduces a major durable architectural decision, also
      create an Architecture Decision Record under <repo>/adr/. The path
      <repo>/adr/ means the target repository's top-level adr/ folder beside
      openspec/, not any folder inside openspec/. Repository-level ADR files
      persist across changes and MUST be written outside openspec/.


      IRON RULE - ADRs are immutable once accepted. You MUST NOT edit a prior
      ADR file under any circumstance, not its Status, not its body, not its
      date. To change a previously accepted decision, record a NEW ADR whose
      Status is "accepted, supersedes ADR-NNNN" and whose `Supersedes:` field
      names the prior one. Design consumers derive what is currently in force
      by walking Supersedes links across the folder. The prior ADR's contents
      stay frozen as a historical record.


      Process:

      1. Read every existing ADR in <repo>/adr/. Build the supersession graph
      from each ADR's Supersedes field. Note the highest sequence number in
      use.

      2. Re-read design.md. Identify decisions that meet ALL of these:

      - Establish a long-term architectural commitment (pattern, technology,
      boundary, contract) - not a tactical implementation detail.

      - Will affect future changes beyond this one.

      - Either aren't already captured by a currently-in-force ADR, OR
      intentionally diverge from one. If diverging, the new ADR supersedes the
      prior.

      3. For each qualifying decision, create a NEW repository-level file at
      <repo>/adr/NNNN-kebab-title.md, where NNNN is a 4-digit sequence one
      greater than the highest existing. Include title, Status, Date, optional
      Supersedes, and MADR-short sections: Context, Decision, and
      Consequences. If this decision replaces a prior in-force ADR, set Status to
      "accepted, supersedes ADR-NNNN", populate the `Supersedes:` field, and
      explain in Context why the prior decision is being revisited. Do NOT
      touch the prior ADR's file.

      4. Write openspec/changes/<change>/adr.md using the template. The
      manifest MUST state that ADR review completed for this change, list the
      in-force ADR context reviewed, and reference every repository-level ADR
      file created for this change. It MUST NOT duplicate full Context,
      Decision, or Consequences content from repository-level ADR files.

      5. If nothing meets the bar, do not invent ADRs. The manifest MUST
      explicitly state that no major durable architectural decisions were
      introduced and no new repository-level ADR files were created.


      Filename convention: NNNN-kebab-title.md (e.g.,
      0042-use-postgres-for-catalog.md). Sequence is monotonic across the
      whole repo and never reused.
    requires:
      - design
  - id: tasks
    generates: tasks.md
    description: Implementation checklist with trackable tasks
    template: tasks.md
    instruction: >
      Create the task list that breaks down the implementation work.


      **IMPORTANT: Follow the template below exactly.** The apply phase parses

      checkbox format to track progress. Tasks not using `- [ ]` won't be
      tracked.


      Guidelines:

      - Group related tasks under ## numbered headings

      - Each task MUST be a checkbox: `- [ ] X.Y Task description`

      - Tasks should be small enough to complete in one session

      - Order tasks by dependency (what must be done first?)


      The template's first-time-setup section is written against whichever

      acceptance stack the project uses. Read `stack:` from
      `openspec/config.yaml`

      (`javascript` or `python`) and substitute it wherever the template says

      `<stack>`, inlining the concrete destination filenames from that stack's

      "Files to copy" table in the acceptance-test-authoring skill

      (`references/<stack>/SETUP.md`). The authored tasks.md must name real

      filenames, not placeholders. If `stack:` is absent, task 1.1 records it

      first — do not guess it.


      Reference specs for what to build, design for how to build it, and

      <repo>/adr/ for durable architectural commitments to honor.

      Each task should be verifiable - you know when it's done.
    requires:
      - specs
      - adr
apply:
  requires:
    - tasks
  tracks: tasks.md
  instruction: |
    Read context files, work through pending tasks, mark complete as you go.
    Pause if you hit blockers or need clarification.
format:
  extension: ".md"
  fence:
    open: '^```gherkin\s*$'
    close: '^```\s*$'
  # requirement, scenario and delta below match lines INSIDE ```gherkin fences:
  requirement: '^\s*Rule:\s*(?<name>.+)$'
  scenario: '^\s*Scenario:'
  delta:
    marker: '@openspec:\s*(?<op>ADDED|MODIFIED|REMOVED|RENAMED)'
    rename: '@openspec:\s*RENAMED\s+from="(?<from>[^"]+)"\s+to="(?<to>[^"]+)"'
